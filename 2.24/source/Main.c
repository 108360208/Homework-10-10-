#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int number;
	printf("叫块计ㄓ耞琌计临琌案计:");
	scanf_s("%d", &number);
	if (number % 2 == 0)
	{
		printf("块计案计\n");
	}
	else
	{
		printf("块计计\n");
	}

	system("pause");
	return 0;
}